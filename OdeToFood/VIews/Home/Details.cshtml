@model Restaurant

@{
    ViewBag.Title = Model.Name;
}

<h1>@Model.Name</h1>

<div>
    Id: @Model.Id
</div>
<div>
    Cuisine: @Model.Cuisine
</div>

<a class="btn btn-default" asp-action="Index">Home</a>
<a class="btn btn-default" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
